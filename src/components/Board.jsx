import Deck from "./Deck";
import CardCounter from "./CardCounter";
import styles from "./Board.module.css"

// WHERE I LEFT OFF
// Working on card counter. Currently on flushes. Got the implementation for 4 card flush, need to refactor for 5 card. 

function Board() {
    return (
        //     <Deck />
        <CardCounter />
    );
  }

  export default Board;